# Frontend — copy to frontend/.env for local dev (optional).
# Leave blank to use the Vite dev proxy (http://localhost:3001).
# In production (Vercel), set this to your deployed Railway backend URL.
VITE_API_URL=
